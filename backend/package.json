{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "node server.js",
    "start-test": "node -e \"const s=require('child_process').spawn('node',['server.js'],{stdio:'inherit',cwd:__dirname});setTimeout(()=>{s.kill();process.exit(0)},10000)\"",
    "db:init": "node scripts/db-init.js",
    "db:dump": "node scripts/db-dump.js",
    "db:restore": "node scripts/db-restore.js",
    "db:seed": "node -e \"require('./db'); const {runSeed}=require('./seed'); runSeed(require('./db')).then(()=>process.exit(0))\"",
    "setup": "npm run db:init && npm run db:seed",
    "test:email": "node scripts/test-email.js",
    "test": "echo \"No tests\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "googleapis": "^171.4.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "mysql2": "^3.19.0",
    "nodemailer": "^9.0.3",
    "resend": "^6.9.3",
    "sharp": "^0.34.5",
    "uuid": "^13.0.0"
  }
}
