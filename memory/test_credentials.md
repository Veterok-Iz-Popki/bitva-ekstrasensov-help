# Test Credentials

## Admin panel
- **URL**: `/admin/login`
- **Username**: `nikoa2020@gmail.com`
- **Password**: `aspire5542gl1952tq`

> Креды можно переопределить через ENV-переменные `ADMIN_USERNAME` и `ADMIN_PASSWORD` в `/app/backend/.env`. При следующем рестарте backend обновит пароль в БД (idempotent — безопасно перезапускать).
